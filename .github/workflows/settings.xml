<settings>
  <servers>
    <server>
      <id>github</id>
      <username>${env.GITHUB_USERNAME}</username>  <!-- Usar la variable de entorno -->
      <password>${env.GITHUB_TOKEN}</password>
    </server>
  </servers>
</settings>